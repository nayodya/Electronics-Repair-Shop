apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: ers-frontend
  labels:
    app: client
spec:
  type: NodePort
  selector:
    app: client
  ports:
  - name: http
    port: 5173
    targetPort: 5173
    nodePort: 30173
    protocol: TCP
