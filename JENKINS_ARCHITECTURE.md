# Jenkins CI/CD Architecture Diagram

## 🏗️ System Architecture Overview

```
┌────────────────────────────────────────────────────────────────────────────┐
│                         GitHub Repository                                  │
│  (https://github.com/nayodya/Electronics-Repair-Shop)                      │
└────────────────────────────────────────────────────────────────────────────┘
                                     │
                      ┌──────────────┼──────────────┐
                      │              │              │
                      ▼              ▼              ▼
                  Push Event    Pull Request    Tag Created
                      │              │              │
                      └──────────────┼──────────────┘
                                     │
                      ┌──────────────▼──────────────┐
                      │   GitHub Webhook            │
                      │   (Triggered Event)         │
                      └──────────────┬──────────────┘
                                     │
                                     ▼
                      ┌──────────────────────────────┐
                      │   Jenkins Master             │
                      │   (Port 8080)                │
                      │   ┌──────────────────────┐   │
                      │   │ Pipeline Orchestrator│   │
                      │   │ - Parse Jenkinsfile  │   │
                      │   │ - Schedule jobs      │   │
                      │   │ - Manage stages      │   │
                      │   └──────────────────────┘   │
                      └──────────┬───────────────────┘
                                 │
         ┌───────────────────────┼───────────────────────┐
         │                       │                       │
         ▼                       ▼                       ▼
    ┌─────────────┐         ┌─────────────┐      ┌──────────────┐
    │ Build Agent │         │Build Agent 2│      │ Build Agent 3│
    │(Docker)     │         │(Kubernetes) │      │(Optional)    │
    └─────────────┘         └─────────────┘      └──────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
    ┌─────────────────────────────────────────────────────────┐
    │              Pipeline Execution Stages                  │
    ├─────────────────────────────────────────────────────────┤
    │ 1. Checkout → Code from GitHub                          │
    │ 2. Build Backend → .NET 8.0 compilation                 │
    │ 3. Build Frontend → React with Vite                     │
    │ 4. Run Tests → Unit, Integration, E2E tests             │
    │ 5. Code Quality → SonarQube analysis                    │
    │ 6. Security Scan → Trivy, SAST, Secret detection       │
    │ 7. Build Images → Docker image creation                │
    │ 8. Push Registry → Docker Hub push                     │
    │ 9. Deploy → Environment deployment                     │
    │ 10. Verify → Smoke tests and health checks             │
    │ 11. Report → Generate and publish reports              │
    └─────────────────────────────────────────────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                        │
                    ▼                        ▼
         ┌────────────────────┐   ┌────────────────────┐
         │  Build Artifacts   │   │  Quality Reports   │
         │  - .NET binaries   │   │  - Test results    │
         │  - React dist      │   │  - Coverage data   │
         │  - Docker images   │   │  - Security scan   │
         │  - Artifacts       │   │  - Performance     │
         └────────────────────┘   └────────────────────┘
                    │                        │
                    ▼                        ▼
         ┌────────────────────┐   ┌────────────────────┐
         │ Docker Registry    │   │ Monitoring Stack   │
         │ (Docker Hub)       │   │ - SonarQube        │
         │ - Backend image    │   │ - Grafana          │
         │ - Frontend image   │   │ - Prometheus       │
         └────────────────────┘   │ - Alerts           │
                    │             └────────────────────┘
                    ▼
         ┌────────────────────┐
         │   Deployment       │
         ├────────────────────┤
         │ Development env    │ (Docker Compose)
         │ Staging env        │ (Kubernetes)
         │ Production env     │ (Kubernetes)
         └────────────────────┘
```

---

## 📊 Service Communication Diagram

```
┌───────────────────────────────────────────────────────────────────┐
│                   Jenkins Ecosystem                               │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────┐                                              │
│  │   Jenkins UI    │  ◄──────────────► GitHub                    │
│  │  :8080          │                                              │
│  └────────┬────────┘                                              │
│           │                                                       │
│           │ (orchestrates)                                        │
│           ▼                                                       │
│  ┌─────────────────────────────────────────┐                     │
│  │   Jenkins Master                        │                     │
│  │   - Job scheduler                       │                     │
│  │   - Pipeline executor                  │                     │
│  │   - Build history                      │                     │
│  └────────┬────────────────────────┬───────┘                     │
│           │                        │                              │
│           ▼                        ▼                              │
│  ┌──────────────────┐    ┌──────────────────┐                   │
│  │ Build Agents     │    │ Jenkins Home     │                   │
│  │ (Execute jobs)   │    │ /var/jenkins     │                   │
│  │                  │    │ (Persisted)      │                   │
│  └────────┬─────────┘    └──────────────────┘                   │
│           │                                                       │
│  ┌────────▼──────────────────────────────────────┐               │
│  │          Docker Execution Layer               │               │
│  │  - Backend builds in Docker                   │               │
│  │  - Frontend builds in Docker                  │               │
│  │  - Tests run in containers                    │               │
│  └────────┬──────────────────────────────────────┘               │
│           │                                                       │
│      ┌────┴────┬─────────┬──────────┐                           │
│      ▼         ▼         ▼          ▼                           │
│   ┌─────┐  ┌─────┐  ┌──────┐  ┌────────┐                       │
│   │.NET │  │React│  │Tests │  │Docker  │                       │
│   │ SDK │  │Vite │  │Suite │  │Images  │                       │
│   └─────┘  └─────┘  └──────┘  └────────┘                       │
│                                                                   │
│  ┌────────────────────────────────────────────────┐              │
│  │   Quality & Security Analysis                  │              │
│  │                                                │              │
│  │  ┌──────────────┐  ┌─────────────────────┐   │              │
│  │  │  SonarQube   │  │  Security Tools     │   │              │
│  │  │  :9000       │  │  - Trivy            │   │              │
│  │  │              │  │  - Semgrep (SAST)   │   │              │
│  │  │ - Coverage   │  │  - Dependency check │   │              │
│  │  │ - Bugs       │  │  - Secret detector  │   │              │
│  │  │ - Vulns      │  └─────────────────────┘   │              │
│  │  └──────────────┘                            │              │
│  │                                                │              │
│  │  ┌─────────────────┐                         │              │
│  │  │  PostgreSQL     │                         │              │
│  │  │  :5432          │ (stores results)        │              │
│  │  └─────────────────┘                         │              │
│  └────────────────────────────────────────────────┘              │
│                                                                   │
│  ┌────────────────────────────────────────────────┐              │
│  │   Monitoring & Visualization                   │              │
│  │                                                │              │
│  │  ┌──────────────────┐  ┌──────────────────┐  │              │
│  │  │  Prometheus      │  │   Grafana        │  │              │
│  │  │  :9090           │  │   :3000          │  │              │
│  │  │                  │  │                  │  │              │
│  │  │ - Metrics        │  │ - Dashboards     │  │              │
│  │  │ - Time series    │  │ - Alerts         │  │              │
│  │  │ - Scraping       │  │ - Annotations    │  │              │
│  │  └──────────────────┘  └──────────────────┘  │              │
│  └────────────────────────────────────────────────┘              │
│                                                                   │
│  ┌──────────────────────────────────────────────┐                │
│  │   Registry & Deployment                      │                │
│  │                                              │                │
│  │  ┌───────────┐  ┌──────────────────────┐   │                │
│  │  │Docker Hub │  │ Deployment Targets   │   │                │
│  │  │           │  │ - Docker Compose    │   │                │
│  │  │ - Images  │  │ - Kubernetes         │   │                │
│  │  │ - Tags    │  │ - Cloud platforms    │   │                │
│  │  └───────────┘  └──────────────────────┘   │                │
│  └──────────────────────────────────────────────┘                │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Build Pipeline Flow

```
START
  │
  ├─► 1. SCM Checkout
  │   └─► Clone git repo (main branch)
  │       ├─► Frontend code
  │       ├─► Backend code
  │       └─► Configuration files
  │
  ├─► 2. Parallel Build
  │   ├─► Build Backend
  │   │   ├─► dotnet restore (NuGet packages)
  │   │   ├─► dotnet build -c Release
  │   │   └─► Run unit tests (if available)
  │   │
  │   └─► Build Frontend
  │       ├─► npm ci (install dependencies)
  │       ├─► npm run lint (ESLint checks)
  │       └─► npm run build (Vite build)
  │
  ├─► 3. Testing
  │   ├─► Backend unit tests
  │   ├─► Backend integration tests
  │   ├─► Frontend unit tests
  │   ├─► E2E tests (optional)
  │   └─► Coverage analysis
  │
  ├─► 4. Code Quality Analysis
  │   ├─► SonarQube scan
  │   │   ├─► Coverage metrics
  │   │   ├─► Code smells
  │   │   └─► Bugs and vulnerabilities
  │   │
  │   └─► Check Quality Gates
  │       ├─► Coverage >= 70%?
  │       ├─► No critical issues?
  │       └─► Pass quality gate?
  │           │
  │           ├─YES─► Continue
  │           └─NO──► FAIL BUILD
  │
  ├─► 5. Security Scanning
  │   ├─► Dependency Check
  │   │   ├─► npm audit
  │   │   └─► dotnet list package --vulnerable
  │   │
  │   ├─► SAST Analysis (Semgrep)
  │   │   └─► Source code vulnerabilities
  │   │
  │   ├─► Secret Detection
  │   │   └─► Exposed credentials
  │   │
  │   └─► Check Security Results
  │       │
  │       ├─PASS─► Continue
  │       └─FAIL─► FAIL BUILD
  │
  ├─► 6. Build Docker Images
  │   ├─► Build backend image
  │   │   └─► Tag: electrons-repair-backend:{build_number}
  │   │
  │   └─► Build frontend image
  │       └─► Tag: electronics-repair-frontend:{build_number}
  │
  ├─► 7. Scan Container Images
  │   ├─► Trivy scan
  │   │   ├─► Check for CVEs
  │   │   └─► Severity: HIGH, CRITICAL
  │   │
  │   └─► If critical found: FAIL BUILD
  │
  ├─► 8. Push to Registry
  │   ├─► Authenticate Docker
  │   ├─► Push backend image
  │   ├─► Push frontend image
  │   └─► Tag latest
  │
  ├─► 9. Deploy
  │   ├─► Select environment (dev/staging/prod)
  │   ├─► Backup current deployment
  │   ├─► Deploy new images
  │   ├─► Database migrations (if needed)
  │   └─► Verify deployment
  │
  ├─► 10. Smoke Tests
  │   ├─► API health check
  │   ├─► Frontend availability
  │   ├─► Database connectivity
  │   └─► If failed: ROLLBACK
  │
  ├─► 11. Performance Testing
  │   ├─► Load test (Apache Bench)
  │   └─► Lighthouse audit (frontend)
  │
  ├─► 12. Generate Reports
  │   ├─► Test results (TRX, JSON)
  │   ├─► Coverage report (HTML)
  │   ├─► Security scan results
  │   └─► Performance metrics
  │
  ├─► 13. Notifications
  │   ├─► Email notification
  │   ├─► Slack notification
  │   └─► GitHub status update
  │
  └─► END (Success or Failure)

FAILURE PATH:
  Any stage fails ──► Stop pipeline
                  ├─► Notify team
                  ├─► Preserve logs
                  └─► Mark build as FAILED
```

---

## 🗄️ Data Flow Diagram

```
Input Data:
  Git Repository ──────┐
                       │
                       ▼
              ┌────────────────────┐
              │  Jenkins Pipeline  │
              │  Processes Code    │
              └────────┬───────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
    Test Data     Artifacts      Quality Data
        │              │              │
        ▼              ▼              ▼
    ┌────────┐  ┌──────────┐  ┌──────────┐
    │ Test   │  │ Build    │  │ Code     │
    │Results │  │ Artifacts│  │ Metrics  │
    └──┬─────┘  └────┬─────┘  └────┬────┘
       │             │             │
       └─────────────┼─────────────┘
                     │
                     ▼
            ┌────────────────────┐
            │  Storage Systems   │
            ├────────────────────┤
            │ Test Reports (TRX) │
            │ Coverage (HTML)    │
            │ Docker Registry    │
            │ SonarQube DB       │
            │ Prometheus DB      │
            └────────┬───────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
    Grafana     Jenkins UI    SonarQube
    Dashboards   Interface     Interface
        │            │            │
        └────────────┼────────────┘
                     │
                     ▼
              User Visibility
         (Reports, Alerts, Metrics)
```

---

## 🔐 Security Architecture

```
┌──────────────────────────────────────────────────────────────┐
│              Security Scanning Architecture                  │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  Input Code                                                  │
│    │                                                          │
│    ├─► Secret Detection                                      │
│    │   └─► Prevent credential leaks                          │
│    │
│    ├─► Source Code Analysis (SAST)                           │
│    │   └─► Semgrep                                           │
│    │       ├─► Common vulnerabilities                        │
│    │       ├─► Security patterns                             │
│    │       └─► Custom rules                                  │
│    │
│    ├─► Dependency Check                                      │
│    │   ├─► npm audit                                         │
│    │   └─► dotnet list --vulnerable                          │
│    │
│    └─► SonarQube Analysis                                    │
│        ├─► Code smells                                       │
│        ├─► Bugs                                              │
│        └─► Vulnerabilities                                   │
│
│  Built Images                                                │
│    │                                                          │
│    └─► Container Scanning (Trivy)                            │
│        ├─► CVE database lookup                               │
│        ├─► Severity assessment                               │
│        └─► Vulnerability blocking                            │
│                                                               │
│  Results:                                                     │
│    ├─► Pass ──► Continue to deployment                       │
│    └─► Fail ──► Block deployment & notify team               │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 📈 Monitoring & Metrics Flow

```
Metrics Collection:
    │
    ├─► Jenkins Metrics
    │   ├─► Build duration
    │   ├─► Success rate
    │   ├─► Queue size
    │   └─► Agent status
    │
    ├─► Application Metrics
    │   ├─► Response time
    │   ├─► Error rates
    │   ├─► CPU usage
    │   └─► Memory usage
    │
    └─► Test Metrics
        ├─► Test count
        ├─► Pass/fail ratio
        ├─► Coverage %
        └─► Execution time

         ▼
    ┌────────────────┐
    │  Prometheus    │
    │  (Scraper)     │
    │  :9090         │
    └────────┬───────┘
             │
    ┌────────▼───────────┐
    │ Time Series DB     │
    │ (Metrics stored)   │
    └────────┬───────────┘
             │
    ┌────────▼───────────┐
    │ Grafana            │
    │ (Visualization)    │
    │ :3000              │
    └────────┬───────────┘
             │
    ┌────────▼───────────┐
    │ Dashboards         │
    ├────────────────────┤
    │ Build Trends       │
    │ Quality Metrics    │
    │ Performance        │
    │ Alerts             │
    └────────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌──────────────────────────────────────────────────────────┐
│          Multi-Environment Deployment                    │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  Docker Registry                                          │
│  (Docker Hub)                                             │
│       │                                                   │
│       ├─► Image: electronics-repair-backend:v1.0          │
│       └─► Image: electronics-repair-frontend:v1.0         │
│                                                           │
│       ▼                                                   │
│  ┌─────────────────────────────────────┐                │
│  │  Deployment Decision Point          │                │
│  │  - Git branch: main/develop/feature │                │
│  │  - Build artifacts available        │                │
│  │  - Quality gates passed             │                │
│  └─────────┬───────────────────────────┘                │
│            │                                              │
│    ┌───────┼───────┬──────────┐                          │
│    │       │       │          │                          │
│    ▼       ▼       ▼          ▼                          │
│ ┌────┐ ┌────┐ ┌────┐ ┌────────┐                        │
│ │Dev │ │Test│ │Stag│ │ Prod   │                        │
│ └────┘ └────┘ └────┘ └────────┘                        │
│    │       │       │          │                          │
│    ▼       ▼       ▼          ▼                          │
│ ┌──────────────────────────────────────────┐             │
│ │  Docker Compose                          │             │
│ │  (Local/Dev environment)                 │             │
│ └──────────────────────────────────────────┘             │
│                                                           │
│ ┌──────────────────────────────────────────┐             │
│ │  Kubernetes Cluster                      │             │
│ │  (Staging/Production)                    │             │
│ │                                          │             │
│ │  ┌─────────────────────────────────────┐ │             │
│ │  │ Namespace: ers-staging              │ │             │
│ │  │ - Backend Pods                      │ │             │
│ │  │ - Frontend Pods                     │ │             │
│ │  │ - Database                          │ │             │
│ │  └─────────────────────────────────────┘ │             │
│ │                                          │             │
│ │  ┌─────────────────────────────────────┐ │             │
│ │  │ Namespace: ers-production           │ │             │
│ │  │ - Backend Pods (replicas)           │ │             │
│ │  │ - Frontend Pods (replicas)          │ │             │
│ │  │ - Database (HA)                     │ │             │
│ │  │ - Load Balancer                     │ │             │
│ │  └─────────────────────────────────────┘ │             │
│ └──────────────────────────────────────────┘             │
│                                                           │
│  Post-Deployment:                                         │
│  ├─► Smoke tests                                          │
│  ├─► Health checks                                        │
│  ├─► Metrics collection                                   │
│  ├─► Alert verification                                   │
│  └─► Team notification                                    │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

---

## 🔗 Integration Points

```
External Systems Integration:

    Git Repository (GitHub)
           │
           ├─► Webhooks (Notify Jenkins)
           │
           └─► Code (Pull source)

    Email Service
           │
           └─► Notifications

    Slack
           │
           └─► Build updates

    Docker Hub
           │
           ├─► Push images
           └─► Pull images

    SonarQube
           │
           └─► Code analysis

    Kubernetes
           │
           └─► Deployment

    Cloud Providers (AWS/Azure)
           │
           └─► Infrastructure

    Monitoring Tools
           │
           └─► Metrics & Alerts
```

---

All diagrams are system design representations showing how the Jenkins CI/CD pipeline integrates with the Electronics Repair Shop project components.
